diff --git a/Makefile.linux b/Makefile.linux
index 7b27008..361fe80 100644
--- a/Makefile.linux
+++ b/Makefile.linux
@@ -20,7 +20,13 @@ COMMANDS += containerd-shim containerd-shim-runc-v1 containerd-shim-runc-v2
 
 # check GOOS for cross compile builds
 ifeq ($(GOOS),linux)
-	GO_GCFLAGS += -buildmode=pie
+	ifeq ($(GOARCH),mips64*)
+		GO_CFLAGS += -buildmode=exe
+	elifeq ($(GOARCH),riscv64)
+		GO_CFLAGS += -buildmode=exe
+	else
+		GO_GCFLAGS += -buildmode=pie
+	endif
 endif
 
 # amd64 supports go test -race

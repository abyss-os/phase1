pkgname=sqlite
pkgver=3.31.1
_pkgver=replication4
pkgrel=2
pkgdesc="C library that implements an SQL database engine"
url="https://www.sqlite.org/"
arch="all"
license="Public-Domain"
makedepends="readline-dev tcl-dev"
subpackages="$pkgname-doc $pkgname-dev"
options="!check"
builddir="$srcdir/sqlite-version-${pkgver}-${_pkgver}"
source="$pkgname-$pkgver.tar.gz::https://github.com/canonical/sqlite/archive/version-${pkgver}+${_pkgver}.tar.gz
	license.txt
	"

build() {
	cd "$builddir"

	./configure \
		--build="$CBUILD" \
		--host="$CHOST" \
		--prefix=/usr \
		--sbindir=/usr/bin \
		--enable-threadsafe \
		--enable-readline \
		--enable-static \
		--enable-dynamic-extensions \
		--enable-fts3 \
		--enable-replication \
		--disable-amalgamation \
		--disable-tcl

	# rpath removal
#	sed -i 's|^hardcode_libdir_flag_spec=.*|hardcode_libdir_flag_spec=""|g' libtool
#	sed -i 's|^runpath_var=LD_RUN_PATH|runpath_var=DIE_RPATH_DIE|g' libtool

	make
}

package() {
	cd "$builddir"

	make -j1 DESTDIR="$pkgdir" install

	install -Dm0644 sqlite3.1 \
		"$pkgdir"/usr/share/man/man1/sqlite3.1
	install -Dm644 "$srcdir"/license.txt \
		"$pkgdir"/usr/share/licenses/$pkgname/license.txt
}

b2sums="a291928b69814d8a9dbda600960f84587bf375437efab3e41a7f25c2cde275b660b8d2a62566a50e328c9f1756174dd97702b937dfab78ef87df8b88c8ce16d0  sqlite-3.31.1.tar.gz
847d2f10831a4a7a2dfb12c142c418fffc74829f3fde24f9a343bb6e0f7b7de110a909ca97da77aaa7462056f8ce009ffbcb86d7ecd3a4f36fa80ee598ca921e  license.txt"

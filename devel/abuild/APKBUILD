pkgname=abuild
pkgver=4.0.58
pkgrel=0
pkgdesc="Script to build Alpine Packages"
url="https://git.alpinelinux.org/cgit/abuild/"
arch="all"
license="GPL-2.0"
depends="sudo pax-utils openssl apk-tools bsdtar
	 attr pkgconf patch bzip2 xz zlib lzip diffutils flex
	 bison unzip gmake file musl-utils pigz gzip
	 libcxx-dev llvm-dev rsync libtool curl pkgconf zlib-dev
	 openssl-dev musl-dev automake autoconf signify"
subpackages="abuild-rootbld:_rootbld:noarch $pkgname-doc"
# tests in abuild aren't released yet
options="suid !check"
source="$pkgname-v$pkgver.tar.gz::https://github.com/abyss-os/abuild/archive/v$pkgver.tar.gz"

builddir="$srcdir/$pkgname-$pkgver"

prepare() {
	default_prepare

	cd "$builddir"
	sed -i -e "/^CHOST=/s/=.*/=$CHOST/" abuild.conf
}

build() {
	cd "$builddir"
	make VERSION="$pkgver-r$pkgrel" CC=$CC
}

package() {
	cd "$builddir"

	make install VERSION="$pkgver-r$pkgrel" DESTDIR="$pkgdir"

	install -m 644 abuild.conf "$pkgdir"/etc/abuild.conf
	install -d -m 775 -g abuild "$pkgdir"/var/cache/distfiles
}

_rootbld() {
	pkgdesc="Build packages in chroot"
	depends="abuild bubblewrap git"
	mkdir -p "$subpkgdir"
}

b2sums="d7d7bf27c5d77cfed465eb27f1d7850c2c828b2aabf3559c1058a3cb6d57a6d7beed0c5cf6cda9d9eac6a3054ae6fde17c4bcedfb425a4924cc6da3741438df3  abuild-v4.0.58.tar.gz"
